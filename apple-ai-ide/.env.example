# Prevent browser from opening automatically
# This is needed because we run the app in Electron, not in a browser
# Set to 'none' to prevent browser from opening
BROWSER=none

# React development settings
GENERATE_SOURCEMAP=true
