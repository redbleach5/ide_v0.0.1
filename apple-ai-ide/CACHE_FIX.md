# 🔧 Исправление проблемы с кешированием

## Проблема
AI начал говорить про змейку, хотя это были тесты с LM Studio. Это указывало на то, что где-то копится старый кеш или контекст из предыдущих сессий/проектов.

## ✅ Исправления

### 1. Очистка кешей при смене проекта
**Файл:** `App.tsx`
- При открытии нового проекта теперь очищаются:
  - `codebaseIndexService` кеш
  - `ragService` кеш (RAG индекс)
  - `inlineCompletionService` кеш
- Проверка, что старый проект отличается от нового перед очисткой

### 2. Очистка кешей при создании новой сессии
**Файл:** `App.tsx`
- При создании новой сессии чата очищается `inlineCompletionService` кеш
- Это гарантирует, что автодополнения не содержат старых данных

### 3. Очистка кешей при смене сессии
**Файл:** `AIPanel.tsx`
- При переключении между сессиями очищается `inlineCompletionService` кеш
- Это предотвращает использование старых данных из других сессий

### 4. Проверка соответствия RAG индекса проекту
**Файл:** `ragService.ts`
- В `getRelevantContext` добавлена проверка, что RAG индекс соответствует текущему проекту
- Если проект изменился, старый индекс автоматически очищается

### 5. Проверка соответствия индекса проекту в AI Service
**Файл:** `aiService.ts`
- Добавлена проверка, что RAG индекс соответствует текущему проекту перед использованием
- Если не соответствует - автоматически очищается
- Проверка соответствия базового индекса проекту

### 6. Фильтрация файлов по проекту
**Файл:** `AIPanel.tsx`
- Во всех режимах (chat, code, analysis) добавлена фильтрация файлов
- Используются только файлы, которые принадлежат текущему проекту
- Это предотвращает попадание файлов из других проектов в контекст

## 🎯 Результат

Теперь:
1. ✅ При смене проекта все кеши очищаются
2. ✅ При создании новой сессии кеши очищаются
3. ✅ При смене сессии кеши очищаются
4. ✅ RAG индекс проверяется на соответствие проекту
5. ✅ В контекст попадают только файлы текущего проекта
6. ✅ Старые данные не влияют на новые запросы

## 🔍 Как проверить

1. Откройте проект с змейкой
2. Создайте новую сессию чата
3. Отправьте сообщение - AI не должен упоминать змейку
4. Откройте другой проект
5. Отправьте сообщение - AI должен работать только с новым проектом

## 📝 Дополнительные улучшения

Если проблема сохраняется:
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что `projectContext.projectPath` правильно передается
3. Проверьте, что файлы в `projectContext.files` принадлежат текущему проекту

