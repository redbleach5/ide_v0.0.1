# 🔍 Диагностика проблемы "Модель не отвечает"

## ✅ Проверено

1. **Ollama запущен** - процесс активен
2. **Модель установлена** - `qwen2.5-coder:1.5b` доступна
3. **API работает** - тестовый запрос успешен

## 🔧 Возможные причины и решения

### 1. Модель не выбрана в настройках IDE

**Проверка:**
- Откройте настройки (⚙️)
- Перейдите на вкладку "ИИ-помощник"
- Проверьте, что в поле "Модель" выбрана `qwen2.5-coder:1.5b`

**Решение:**
- Нажмите "Обновить список моделей"
- Выберите модель из списка
- Или введите название вручную: `qwen2.5-coder:1.5b`

### 2. Неправильный endpoint

**Проверка:**
- В настройках проверьте поле "Ollama Endpoint"
- Должно быть: `http://localhost:11434`

**Решение:**
- Убедитесь, что endpoint правильный
- Нажмите "Проверить" для тестирования подключения

### 3. Таймаут слишком короткий

**Проблема:** Модель отвечает медленно (2-3 секунды), а таймаут может быть слишком коротким.

**Решение:** Увеличить таймаут в настройках или проверить логи в консоли браузера.

### 4. Проблема с function calling

**Проблема:** Если модель не поддерживает function calling, запрос может завершиться ошибкой.

**Решение:** 
- Проверьте, поддерживает ли модель function calling
- Попробуйте отключить function calling для теста

### 5. Проблема с форматом запроса

**Проверка:** Откройте консоль браузера (F12) и проверьте ошибки в Network или Console.

## 🛠️ Быстрая диагностика

### Шаг 1: Проверьте настройки
1. Откройте настройки (⚙️)
2. Вкладка "ИИ-помощник"
3. Провайдер: **Ollama**
4. Endpoint: `http://localhost:11434`
5. Модель: `qwen2.5-coder:1.5b`
6. Нажмите **"Проверить"** - должно быть "✓ Подключение успешно"

### Шаг 2: Проверьте консоль браузера
1. Откройте DevTools (F12 или Cmd+Option+I)
2. Вкладка Console
3. Отправьте сообщение в AI панели
4. Проверьте ошибки

### Шаг 3: Проверьте Network запросы
1. В DevTools откройте вкладку Network
2. Отправьте сообщение
3. Найдите запрос к `/api/chat`
4. Проверьте статус и ответ

## 📝 Команды для проверки

```bash
# Проверить, запущен ли Ollama
ps aux | grep ollama

# Проверить доступные модели
curl http://localhost:11434/api/tags

# Тестовый запрос к модели
curl -X POST http://localhost:11434/api/chat \
  -d '{"model":"qwen2.5-coder:1.5b","messages":[{"role":"user","content":"Привет"}],"stream":false}' \
  -H "Content-Type: application/json"
```

## 🐛 Частые ошибки

### "Модель не выбрана"
- **Решение:** Выберите модель в настройках

### "Connection refused"
- **Решение:** Убедитесь, что Ollama запущен

### "Timeout"
- **Решение:** Увеличьте таймаут или используйте меньшую модель

### "Model not found"
- **Решение:** Установите модель: `ollama pull qwen2.5-coder:1.5b`

## 💡 Советы

1. **Начните с простого запроса** - "Привет" или "Как дела?"
2. **Проверьте логи** - откройте консоль браузера для диагностики
3. **Перезапустите Ollama** - иногда помогает перезапуск
4. **Проверьте модель** - убедитесь, что модель действительно установлена

