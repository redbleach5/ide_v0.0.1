# Как открыть DevTools для диагностики

## ✅ Рабочие методы (проверены):

### Метод 1: F12 (самый простой)
1. Откройте приложение
2. Нажмите клавишу **F12**
3. DevTools откроется автоматически

### Метод 2: Через меню в приложении
1. Кликните на меню **"Вид"** (иконка глаза в верхней панели)
2. Выберите **"Инструменты разработчика"**
3. DevTools откроется

### Метод 3: Запуск в браузере (если Electron не работает)
1. Откройте терминал
2. Выполните:
   ```bash
   cd apple-ai-ide
   npm run browser
   ```
3. Приложение откроется в браузере
4. Нажмите **F12** в браузере
5. Или используйте:
   - **Mac**: `Cmd+Option+I`
   - **Windows/Linux**: `Ctrl+Shift+I`

## 📋 После открытия DevTools:

1. Перейдите на вкладку **Console**
2. Откройте файл `diagnose-in-app.js`
3. Скопируйте **весь** его содержимое
4. Вставьте в консоль
5. Нажмите `Enter`
6. Подождите 2-3 секунды
7. Скопируйте **весь** вывод
8. Предоставьте его для анализа

## ⚠️ Если ничего не работает:

Выполните в консоли браузера (если запустили через `npm run browser`):

```javascript
// Быстрая проверка
const btn = document.querySelector('[data-menu-button]');
console.log('Кнопка:', !!btn);
if (btn) {
  btn.click();
  setTimeout(() => {
    const menu = document.querySelector('[data-menu-dropdown]');
    console.log('Меню:', !!menu);
    if (menu) {
      const newProject = Array.from(menu.querySelectorAll('button')).find(b => 
        b.textContent.includes('Новый проект')
      );
      console.log('"Новый проект":', !!newProject);
      if (newProject) {
        newProject.click();
        setTimeout(() => {
          const dialog = Array.from(document.querySelectorAll('*')).find(el => 
            el.textContent && el.textContent.includes('Создать новый проект')
          );
          console.log('Диалог:', !!dialog);
        }, 500);
      }
    }
  }, 500);
}
```

Скопируйте весь вывод и предоставьте его.
